#include<stdio.h>
#include<math.h>

int main(){
    double A = sqrt(9);
    double B = pow(2,3);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(2.99);
    double F = fabs(-100);
    double G;
    double H;
    double I;
    double J = tan(45);

    printf("\n%lf", J);

    return 0;

}