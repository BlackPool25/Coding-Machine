#include<stdio.h>

int main(){
    int x = -1, y = 10;

    if(x>0)
    if(y>0)
    printf("X and Y is positive.");
    else
    printf("Y is negative");
    else
    printf("X is not postive");

    return 0;
}